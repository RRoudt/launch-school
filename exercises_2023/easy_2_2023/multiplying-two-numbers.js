/* Easy 2.3: Multiplying Two Numbers

Inputs:
- Number one
- Number two

Output:
- Number one multiplied by number two

Pseudocode:
GET number1
GET number2
RETURN number1 * number2
*/

const multiply = (number1, number2) => number1 * number2;

console.log(multiply(5, 3) === 15); // logs true