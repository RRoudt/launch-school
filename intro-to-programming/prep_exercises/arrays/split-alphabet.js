/* Exercise 4: Split the string alphabet into an array of characters. */

const alphabet = 'abcdefghijklmnopqrstuvwxyz';

const alphabetArray = alphabet.split('');

console.log(alphabet);
console.log(alphabetArray);